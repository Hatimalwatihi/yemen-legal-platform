# سجل التقوية الأمني

هذه النسخة لا تعتبر تدقيقاً أمنياً خارجياً، لكنها تعالج ثغرات واضحة ظهرت في النسخة السابقة:

1. لا توجد حسابات مدير بكلمات مرور ثابتة داخل الكود.
2. حساب المدير الأول يُنشأ فقط عند ضبط `BOOTSTRAP_ADMIN_USERNAME` و`BOOTSTRAP_ADMIN_PASSWORD` في بيئة الخادم.
3. رمز التسجيل يُضبط عبر `LEGAL_INVITE_CODE` بدلاً من رموز عامة معروفة.
4. صلاحية `isAdmin` لا تُستمد من `role` القادم من الهاتف.
5. رموز الجلسات تُنشأ عبر `crypto.randomBytes(32)`.
6. الجلسة الفارغة لا تمنح وصولاً تلقائياً.
7. رفع الملفات يتطلب جلسة مصادقاً عليها.
8. تنزيل الملفات يتحقق من ارتباط اسم الملف ببيانات المستخدم لمنع IDOR الأساسي.
9. P2P يتحقق من أن هوية المرسل تطابق جلسة المصادقة.
10. APK يستطيع استخدام API خارجي عبر `VITE_API_BASE_URL` دون وضع مفتاح Gemini في التطبيق.

ينبغي في الإنتاج إضافة HTTPS فعلي، قاعدة بيانات مُدارة، نسخ احتياطية مشفرة، إدارة أسرار، مراقبة، واختبار اختراق مستقل.
